# Dependencies
node_modules/
.pnp.*
.yarn/install-state.gz

# Build outputs
dist/
build/
.next/
.expo/
.turbo/
out/
*.tsbuildinfo

# Testing
coverage/
*.lcov

# Environment — NEVER commit secrets
.env
.env.local
.env.*.local
.env.development.local
.env.production.local
.env.test.local
!.env.example

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Supabase local
supabase/.branches
supabase/.temp

# Generated types — committed but regenerated, so do NOT ignore
# packages/db-types/src/database.types.ts (committed)

# Photos used by seed scripts — committed (small, version-controlled)
# supabase/seed/photos/ (committed)
